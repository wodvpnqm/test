package com.qm.algorithms.algorithm621;

/**
 * Created by wodvpn on 2017/6/24.
 */
public class Solution {
    /**
     *
     * 621. Task Scheduler
     *
     * 给定一个字符数组代表了CPU需要执行的任务.
     * 它包含了大写字母A到Z,不同的字母代表不同的任务.
     * 任务可以有与原始的顺序不同的执行顺序.
     * 每个任务可以在一个间隔内执行.
     * 对于每个间隔,CPU可以完成一个任务或者保持空闲.
     *
     *
     * @param tasks
     * @param n
     * @return
     */
    public int leastInterval(char[] tasks, int n) {
        return 0;
    }
}
